# 后台角色权限、视觉收口与系统验收推进计划

> 工作区：`D:\00_Project\jiaoyu_lxfw`  
> 用法：每个大阶段建议开一个新线程执行。复制对应阶段的提示词即可。

## 总体顺序

```text
账号登录与角色权限结构确认
→ 角色结构实现与基础验证
→ 同步更新验收计划
→ 登录页与后台整体视觉收口
→ 正式系统功能验收
```

当前不建议直接进入登录页和后台视觉精修。原因是登录页、侧边栏、顶部角色标识、默认工作台、URL 越权和权限边界都会受到角色结构影响。如果先做视觉，再改角色结构，登录页和后台外壳大概率返工。

## 目标角色模型

```text
管理员 = 全部后台界面权限
管理者 = 管理者工作台 + 员工工作台
老师 = 老师工作台 + 员工工作台
顾问 = 顾问工作台 + 员工工作台
员工 = 员工工作台
学生 = 学生工作台
```

说明：

- 员工工作台是内部通用能力包，例如日报、组织架构、新人指南、受控客户查询。
- 管理者、老师、顾问本质上也是内部员工，所以拥有员工工作台合理。
- 学生不是内部员工，只能访问学生服务台。
- 管理员负责系统治理和全部后台访问。

## 影响程度

| 项目 | 影响程度 | 说明 |
| --- | --- | --- |
| 现有业务功能 | 中 | 业务本身大多不变，但入口和权限预期会变 |
| 测试计划 | 中高 | 必须补账号登录、复合角色、越权和侧边栏可见性测试 |
| 登录页视觉 | 高 | 登录方式变化会直接影响页面结构，必须等角色登录结构稳定后再精修 |
| 后台整体视觉 | 中 | 侧边栏和顶部角色结构稳定后再做更稳 |
| 后端/数据库 | 中到高 | 取决于是否做真实账号、角色绑定和接口级权限校验 |

## 全局规则

1. 使用简体中文沟通。
2. 先阅读并遵守 `AGENTS.md`。
3. 修改前必须说明：假设、取舍、成功标准。
4. 官网界面已满意，除非用户明确要求，不要调整官网首页、官网栏目、公开内容和官网业务动线。
5. 用户是小白，每个阶段先说明会调用哪些 skill / 工具，以及它们分别做什么。
6. 如果用户选择的方案会明显影响测试，例如改路由、菜单结构、表单字段、按钮行为、权限入口、接口调用、数据状态流，必须主动提醒风险。
7. 普通业务页不要出现“真实 API / fallback / seed / 原型数据 / OpenAPI / V2/V3”等实现话术。
8. 纯文档改动不要跑前后端测试；只做 `git diff --check`，然后中文 commit。
9. 前端视觉或前端逻辑改动后运行：
   - `cd frontend && npm.cmd run build`
   - `git diff --check`
10. 改后端或权限接口后运行：
   - `cd backend && python -m pytest -v`
   - `cd frontend && npm.cmd run build`
   - `git diff --check`
11. 每个阶段完成后创建中文 Git commit。

---

## 阶段 0：确认 skill、工具和执行方式

### 阶段目标

让下个线程先明确会使用哪些 skill / 工具，避免用户不知道为什么要先设计、再实现、再验证。

### 成功标准

- 线程先说明会调用哪些 skill / 工具。
- 线程先提醒：如果选择大改权限结构，会影响测试计划和视觉顺序。
- 不直接改代码。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请先不要改代码。我要开始做“账号登录 + 角色权限结构收口 + 后台视觉前置治理”。

前置背景：
1. 官网界面我满意，本轮不要调整官网首页、官网栏目、公开内容和官网业务动线。
2. 我是小白，请先确认你会调用哪些合适的 skill / 工具，以及它们分别用来做什么，再引导我选择。
3. 如果我的选择会大幅影响测试，例如改路由、菜单结构、表单字段、按钮行为、权限入口、接口调用、数据状态流，请主动提醒风险。

目标角色模型：
- 管理员 = 全部后台界面权限。
- 管理者 = 管理者工作台 + 员工工作台。
- 老师 = 老师工作台 + 员工工作台。
- 顾问 = 顾问工作台 + 员工工作台。
- 员工 = 员工工作台。
- 学生 = 学生工作台。

请先做：
1. 阅读 AGENTS.md。
2. 说明你会调用哪些 skill / 工具，例如需求梳理、前端设计、浏览器截图、代码搜索、验证流程等。
3. 用小白能理解的方式说明：为什么建议先完成角色结构，再做视觉，再跑正式测试。
4. 给出 2-3 个实现深度选项：
   - 轻量前端角色守卫。
   - 前端角色守卫 + 固定测试账号。
   - 前后端接口级权限一起收口。
5. 标注每个选项对测试计划、视觉调整、后端/数据库的影响程度。
6. 等我确认选项后，再进入下一步。
```

---

## 阶段 1：角色权限结构设计确认

### 阶段目标

把账号、角色、工作台权限、默认入口、可见菜单、URL 越权策略先设计清楚。

### 成功标准

- 明确每个账号登录后进入哪个默认工作台。
- 明确复合角色如何看到员工工作台。
- 明确学生不能访问内部员工、老师、管理者、管理员页面。
- 明确管理员可访问全部后台界面。
- 明确 URL 手动越权时如何处理。
- 先输出设计，不直接写代码。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请做“账号登录与角色权限结构设计确认”，先不要改代码。

必须遵守：
1. 简体中文沟通。
2. 先阅读 AGENTS.md。
3. 修改前先说明假设、取舍和成功标准。
4. 官网界面已满意，本轮不要调整官网。
5. 我是小白，请用表格解释清楚。

目标角色模型：
- 管理员 = 全部后台界面权限。
- 管理者 = 管理者工作台 + 员工工作台。
- 老师 = 老师工作台 + 员工工作台。
- 顾问 = 顾问工作台 + 员工工作台。
- 员工 = 员工工作台。
- 学生 = 学生工作台。

请输出一份设计方案，至少包含：
1. 测试账号清单：账号、显示名称、主角色、可访问工作台、默认入口。
2. 权限矩阵：每个角色能看到哪些后台模块。
3. 登录后跳转规则。
4. 侧边栏显示规则。
5. 顶部角色/身份显示规则。
6. 手动 URL 越权处理规则。
7. 刷新后如何保持当前角色、页面、对象 ID。
8. 对现有测试计划的影响清单。
9. 对后续视觉调整的影响清单。
10. 哪些属于本阶段必须做，哪些可以等功能验收后再做。

先给设计和风险说明，等我确认后再进入实现。
```

---

## 阶段 2：实现账号登录与角色权限结构

### 阶段目标

在不改业务功能的前提下，实现账号登录进入对应权限工作台，并让复合角色拥有员工工作台入口。

### 成功标准

- 登录页支持固定测试账号或账号选择。
- 管理员可访问全部后台。
- 管理者、老师、顾问可访问自己的工作台和员工工作台。
- 员工只访问员工工作台。
- 学生只访问学生工作台。
- 非授权角色手动改 URL 时，不展示无权限业务数据。
- 刷新后角色、页面、关键对象上下文不丢。
- 不改业务表单字段、不改业务接口语义、不改官网。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请实现“账号登录与角色权限结构收口”。这一步可以改代码，但只改和登录、角色权限、后台入口直接相关的文件。

已确认目标角色模型：
- 管理员 = 全部后台界面权限。
- 管理者 = 管理者工作台 + 员工工作台。
- 老师 = 老师工作台 + 员工工作台。
- 顾问 = 顾问工作台 + 员工工作台。
- 员工 = 员工工作台。
- 学生 = 学生工作台。

必须遵守：
1. 简体中文沟通。
2. 先阅读 AGENTS.md。
3. 修改前说明假设、取舍、成功标准。
4. 不改官网界面和官网业务动线。
5. 不改业务表单字段、提交逻辑、数据状态流。
6. 普通业务页不要出现“真实 API / fallback / seed / 原型数据 / OpenAPI / V2/V3”等实现话术。
7. 如果发现需要后端接口级权限才能彻底完成，先说明影响，不要擅自大改。

请执行：
1. 先用 rg 搜索登录页、角色配置、导航配置、后台 Shell、权限守卫相关文件。
2. 给出将要修改的文件清单和原因。
3. 实现固定测试账号或账号登录入口。
4. 实现角色可访问工作台矩阵。
5. 实现侧边栏只显示当前账号允许访问的模块。
6. 实现默认入口和刷新恢复。
7. 实现手动 URL 越权时回到允许入口或展示业务化无权限提示，不展示敏感数据。
8. 完成后运行：
   - cd frontend && npm.cmd run build
   - git diff --check
9. 如果改了后端权限接口，再额外运行：
   - cd backend && python -m pytest -v
10. 验证通过后创建中文 Git commit。
```

---

## 阶段 3：同步更新系统验收计划

### 阶段目标

把新的账号登录、复合角色权限、员工工作台继承、URL 越权规则同步到验收计划，避免正式测试时按旧规则验收。

### 成功标准

- 测试计划包含账号登录用例。
- 测试计划包含复合角色访问员工工作台用例。
- 测试计划包含学生隔离和管理员全量权限用例。
- 测试对象认领表不被打乱，只补必要说明。
- 纯文档改动不跑前后端测试。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请同步更新系统验收计划，只改文档，不改代码。

前置条件：账号登录与角色权限结构已经确定或已经实现。

必须遵守：
1. 简体中文沟通。
2. 先阅读 AGENTS.md。
3. 修改前说明假设、取舍、成功标准。
4. 只改和验收计划直接相关的文档。
5. 纯文档改动不要跑前后端测试。

请检查并更新：
- docs/business-flow-test-plan.md
- docs/test-object-claim-table.md

需要补充的验收点：
1. 固定测试账号登录。
2. 管理员可访问全部后台界面。
3. 管理者可访问管理者工作台和员工工作台。
4. 老师可访问老师工作台和员工工作台。
5. 顾问可访问顾问工作台和员工工作台。
6. 员工只访问员工工作台。
7. 学生只访问学生工作台。
8. 手动 URL 越权不展示无权限业务数据。
9. 刷新后保持当前账号、角色、页面和关键对象上下文。
10. 侧边栏和总览卡入口与权限矩阵一致。

完成后只运行：
- git diff --check

然后创建中文 Git commit。
```

---

## 阶段 4：登录页与后台整体视觉方案确认

### 阶段目标

在角色结构稳定后，先输出 3 套视觉方向，让用户确认后再改代码。

### 成功标准

- 至少 3 套整体风格方案。
- 明确每套方案改哪些区域、不碰哪些业务逻辑。
- 明确风险等级。
- 等用户确认后再实现。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请做“登录页最终视觉 + 后台整体视觉收口”的方案确认，先不要改代码。

前置条件：
1. 官网界面已满意，不调整官网。
2. 账号登录与角色权限结构已经完成或已确认。
3. 现在只做视觉方案，不改业务逻辑。
4. 我是小白，请先说明你会调用哪些 skill / 工具，以及它们分别做什么。

必须遵守：
1. 简体中文沟通。
2. 先阅读 AGENTS.md。
3. 修改前说明假设、取舍、成功标准。
4. UI 效果图阶段至少输出 3 套整体风格方案，我确认后再动代码。
5. 不要大面积重构，不要改后端，不要改业务逻辑。
6. 不调整角色入口、权限结构、表单字段、接口调用、状态流转。

请先做：
1. 用浏览器或截图方式审查当前登录页和后台主要页面。
2. 输出当前视觉问题清单：登录页、后台 Shell、侧边栏、顶部栏、卡片、表格、按钮、移动端。
3. 输出 3 套视觉方向：
   - 方案 A：稳重教育服务型。
   - 方案 B：高密度运营后台型。
   - 方案 C：轻量现代 SaaS 型。
4. 每套方案说明：
   - 适合什么感觉。
   - 会改哪些区域。
   - 不会碰哪些业务逻辑。
   - 对后续功能验收的风险等级。
5. 等我确认一套后，再进入实现。
```

---

## 阶段 5：实现登录页与后台整体视觉收口

### 阶段目标

只改低风险视觉层，让登录页和后台整体更统一、更正式、更顺眼，但不影响业务验收。

### 成功标准

- 登录页像正式系统入口，不像临时演示页。
- 后台 Shell、侧边栏、顶部栏、通用卡片、表格、按钮、标签、空状态风格统一。
- 老师、员工、管理者、管理员、学生之间视觉一致。
- 业务验收路径、按钮行为、接口调用、状态流转不变。
- 前端 build 通过，diff check 通过。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请按照我已确认的视觉方案，实现“登录页最终视觉 + 后台整体视觉收口”。

必须遵守：
1. 简体中文沟通。
2. 先阅读 AGENTS.md。
3. 修改前说明假设、取舍、成功标准。
4. 只改低风险视觉层，不改业务逻辑。
5. 不改官网界面和官网业务动线。
6. 不改角色权限结构、菜单含义、表单字段、接口调用、状态流转。
7. 普通业务页不要出现“真实 API / fallback / seed / 原型数据 / OpenAPI / V2/V3”等实现话术。

优先修改范围：
- 登录页布局、背景、品牌感、账号登录卡片、输入框、错误提示、移动端适配。
- 后台 Shell、侧边栏、顶部栏、通用卡片、表格、按钮、标签、间距、空状态、字体和颜色。
- 老师、员工等页面如果有明显错位、留白、卡片高度不齐，只做低风险样式修正；涉及结构重排先暂停确认。

请执行：
1. 先列出将修改的文件清单和原因。
2. 实现视觉调整。
3. 用浏览器检查登录页和各角色后台关键页面。
4. 运行：
   - cd frontend && npm.cmd run build
   - git diff --check
5. 验证通过后创建中文 Git commit。
```

---

## 阶段 6：正式系统功能验收

### 阶段目标

在角色结构和视觉都稳定后，按完整测试计划进行系统功能验收。

### 成功标准

- 5 人按对象认领表并行验收。
- 前端、后端、数据库、权限、跨角色业务闭环都通过或形成明确缺陷清单。
- 所有阻断和高优先级问题先修复再进入下一轮。

### 可直接复制的提示词

```text
工作区：D:\00_Project\jiaoyu_lxfw

请按现有系统功能验收计划组织正式验收，不要先改视觉。

必须阅读：
- docs/business-flow-test-plan.md
- docs/test-object-claim-table.md
- AGENTS.md

请先做：
1. 汇总验收前置条件。
2. 检查角色账号、权限结构、登录入口是否与测试计划一致。
3. 检查 5 人对象认领表是否足够执行。
4. 给出正式验收执行顺序。
5. 如果发现测试计划与当前代码不一致，先列出差异，不要直接改。

验收重点：
1. 官网入口和登录入口。
2. 账号登录与角色权限。
3. 管理员全后台权限。
4. 管理者、老师、顾问拥有员工工作台。
5. 员工只拥有员工工作台。
6. 学生只拥有学生工作台。
7. 跨角色业务闭环。
8. 单页面搜索、筛选、提交、刷新、详情、空状态、错误状态。
9. 后端 API、数据库持久化、权限边界。
10. 普通业务页文案合规。

正式验证命令按测试计划执行。
```

---

## 推荐执行总览

| 顺序 | 阶段 | 是否改代码 | 是否需要用户确认 |
| --- | --- | --- | --- |
| 1 | 阶段 0：确认 skill、工具和执行方式 | 否 | 是 |
| 2 | 阶段 1：角色权限结构设计确认 | 否 | 是 |
| 3 | 阶段 2：实现账号登录与角色权限结构 | 是 | 实现前确认范围 |
| 4 | 阶段 3：同步更新系统验收计划 | 文档 | 文档完成后确认 |
| 5 | 阶段 4：登录页与后台整体视觉方案确认 | 否 | 是，必须确认 3 选 1 或混合方案 |
| 6 | 阶段 5：实现登录页与后台整体视觉收口 | 是 | 实现前确认范围 |
| 7 | 阶段 6：正式系统功能验收 | 视情况 | 发现差异先确认 |

## 最重要的边界提醒

如果某个线程提出“顺便把菜单重排、路由重做、表单字段调整、接口权限全部重写、业务页重构”，要先暂停。那些都可能影响正式验收，应拆成单独阶段，并先说明测试计划如何同步调整。
